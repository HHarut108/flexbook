{
  "name": "fast-travel-assistant",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:admin": "npm run dev --workspace=admin",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:admin": "npm run build --workspace=packages/shared && npm run build --workspace=admin",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
