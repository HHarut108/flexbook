# VITE_API_URL intentionally omitted — production uses the /api Vercel proxy
# so session cookies are sent same-origin. Direct Render URL bypasses the proxy
# and causes 401s due to cross-origin cookie blocking.
